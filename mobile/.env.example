# API Configuration
# By default, the app uses window.location.origin on web (same domain as deployment)
# Set this if you need to point to a different backend server
# REACT_APP_API_URL=https://your-backend-server.com
